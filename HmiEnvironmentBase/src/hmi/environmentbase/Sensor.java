package hmi.environmentbase;

/**
 * A Sensor reads values from the outside world and provides the (may be post-processed)
 * results of these values. 
 * @author welberge
 */
public interface Sensor
{
    String getId();
}
